const element = document.getElementById('btn')

     // always checking if the element is clicked, if so, do alert('hello')
     element.addEventListener("click", () => {
     	window.location="https://remotedesktop.google.com/headless";
     });
